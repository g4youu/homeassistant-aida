# Home Assistant add-ons intentionally track rolling package versions from the
# base image and npm, so the "pin every version" rules are noise here.
ignored:
  - DL3018  # apk: pin versions
  - DL3016  # npm: pin versions
  - DL3013  # pip: pin versions
  - DL3008  # apt: pin versions
  - DL3006  # FROM ${BUILD_FROM}: tag lives in the ARG (required HA add-on pattern)
